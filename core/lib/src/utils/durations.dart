class Microseconds extends Duration {
  const Microseconds(int microseconds) : super(microseconds: microseconds);
}

class Millis extends Duration {
  const Millis(int millis) : super(milliseconds: millis);
}

class Seconds extends Duration {
  const Seconds(int seconds) : super(seconds: seconds);
}

class Minutes extends Duration {
  const Minutes(int minutes) : super(minutes: minutes);
}

class Hours extends Duration {
  const Hours(int hours) : super(hours: hours);
}

class Days extends Duration {
  const Days(int days) : super(days: days);
}